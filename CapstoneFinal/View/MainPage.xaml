<Page
    x:Class="SpaceInvaders.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SpaceInvaders.Views"
    xmlns:controls="using:SpaceInvaders.Views.Controls"
    Background="Black">

    <Grid>
        <!-- Damos um nome a cada controle para poder acessá-los no C# -->
        <!-- A visibilidade não usa mais Binding, será controlada manualmente -->

        <controls:MenuControl x:Name="MenuScreen" Visibility="Collapsed"/>
        <controls:GameCanvasControl x:Name="GameCanvasScreen" Visibility="Collapsed"/>
        <controls:GameOverControl x:Name="GameOverScreen" Visibility="Collapsed"/>
        <controls:HighScoresControl x:Name="HighScoresScreen" Visibility="Collapsed"/>
        <controls:ControlsControl x:Name="ControlsScreen" Visibility="Collapsed"/>

        <!-- Os recursos ainda são necessários para o GameCanvasControl -->
        <Grid.Resources>
            <local:EntityToSkinConverter x:Key="EntityToSkinConverter"/>
        </Grid.Resources>
    </Grid>
</Page>
